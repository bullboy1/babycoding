# babycoding
This protocol is like a shared "tool inventory" for the project. Before writing any code, the AI must check a registry file (FUNCTIONS.md) for an existing function to reuse or extend — only creating new ones when nothing fits. New functions get logged back into the registry.
